#include "main.h"

/**
 * mul - multiplies two intergers
 * @a: the first number to multiply
 * @b: the second number to multiply with first number
 * Return: the result of multiplying the a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
